name: Build and Deploy Code

# on:
#   push:
#     branches:
#       - "main"
#       - "test_main"
#   pull_request:
#     branches:
#       - "test_main"
on: [push, pull_request]

jobs:
  build:
    environment:
      name: Dev
    env:
      DATABASE_HOSTNAME: ${{ secrets.DATABASE_HOSTNAME }}
      DATABASE_PORT: ${{ secrets.DATABASE_PORT }}
      DATABASE_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
      DATABASE_NAME: ${{ secrets.DATABASE_NAME }}
      DATABASE_USERNAME: ${{ secrets.DATABASE_USERNAME }}
      SECRET_KEY: ${{ secrets.SECRET_KEY }}
      ENCRYPTION_ALGORITHM: ${{ secrets.ENCRYPTION_ALGORITHM }}
      TOKEN_EXPIRATION_MINUTES: ${{ secrets.TOKEN_EXPIRATION_MINUTES }}
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
          POSTGRES_DB: ${{ secrets.DATABASE_NAME }}_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v4
      - name: install python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: update pip
        run: python -m pip install --upgrade pip
      - name: install dependencies
        run: pip install -r requirements.txt
      - name: run tests
        run: |
          pip install pytest
          pytest
      # - name: login to docker hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_HUB_USERNAME }}
      #     password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v3

      # - name: Build
      #   uses: docker/build-push-action@v6
      #   with:
      #     context: ./
      #     push: true
      #     tags: suman1415/fastapi-suman:latest
      #     cache-from: type=registry,ref=suman1415/fastapi:latest
      #     cache-to: type=inline
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: 
      name: production
    steps:
      - name: pulling git repo
        uses: actions/checkout@v4
      - name: deploying to heroku
        uses: akhileshns/heroku-deploy@v3.13.15 # This is the action
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: ${{secrets.HEROKU_APP_NAME}} #Must be unique in Heroku
          heroku_email: ${{secrets.HEROKU_EMAIL}}
        # pull our github repo
        # install heroku cli
        # login to heroku
        # add git remote to heroku
        # push to heroku

         
      
          
       
